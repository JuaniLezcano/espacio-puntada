# Copiar a .env y ajustar si hace falta.
# Desarrollo local (Postgres levantado con `docker compose up -d`):
DATABASE_URL="postgresql://espacio_puntada:espacio_puntada@localhost:5432/espacio_puntada?schema=public"
